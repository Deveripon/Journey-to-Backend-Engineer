# 🧵 Node.js Worker Threads: Multi-Threading in Backend

## 🧐 Worker Threads কি? এটি কেন প্রয়োজন?

আমরা জানি Node.js ডিফল্টভাবে একটি সিঙ্গেল-থ্রেডেড (Single-threaded) রানটাইম। এর মানে হলো, এটি একসাথে কেবল একটি কাজই করতে পারে। যখন কোনো ভারী গাণিতিক কাজ (CPU-intensive task) যেমন—ইমেজ প্রসেসিং, বড় লুপ চালানো বা ডেটা এনক্রিপশন করা হয়, তখন মেইন থ্রেড ব্লক হয়ে যায়। ফলে সার্ভারের অন্য সব রিকোয়েস্ট আটকে থাকে।

এই সমস্যা সমাধানের জন্য Node.js-এ **Worker Threads** মডিউল ব্যবহার করা হয়। এটি ব্যাকগ্রাউন্ডে আলাদা একটি থ্রেডে কাজ চালিয়ে মেইন থ্রেডকে ফ্রি রাখে।

---

## 🚀 The Problem: Blocking the Event Loop

ধরা যাক আপনার সার্ভারে একটি `/heavy` রাউট আছে যেখানে ১০ বিলিয়ন পর্যন্ত একটি লুপ চলে। আপনি যদি সরাসরি মেইন থ্রেডে এই লুপটি চালান:

- ওই সময় অন্য কোনো ইউজার আপনার সার্ভারে লগইন বা হোম পেজ এক্সেস করতে পারবে না।
- জাভাস্ক্রিপ্টের ইভেন্ট লুপ (Event Loop) আটকে থাকবে যতক্ষণ না লুপটি শেষ হয়।

**Worker Thread** আমাদের মেইন থ্রেড থেকে এই ভারী কাজটিকে সরিয়ে অন্য একটি থ্রেডে পাঠিয়ে দেয়, যার ফলে সার্ভার সচল থাকে।

---

## 🛠️ How it Works?

Node.js এর `worker_threads` মডিউলে মূলত দুটি প্রধান জিনিস থাকে:

1. **Main Thread:** যেখান থেকে নতুন ওয়ার্কার তৈরি করা হয়।
2. **Worker Thread:** যেখানে ভারী কাজগুলো ব্যাকগ্রাউন্ডে সম্পন্ন হয়।

এদের মধ্যে ডাটা আদান-প্রদান করা হয় `postMessage()` এবং `onmessage()` বা `parentPort` এর মাধ্যমে।

---

## 💻 Implementation Example

এখানে প্রোজেক্টে যেভাবে ইমপ্লিমেন্ট করা হয়েছে:

### 1. Main Server (`server.js`)

মেইন থ্রেড থেকে আমরা নতুন ওয়ার্কার তৈরি করছি এবং রেজাল্ট পাওয়ার জন্য অপেক্ষা করছি।

```javascript
import { Worker } from 'worker_threads';

app.get('/heavy', (req, res) => {
    // ব্যাকগ্রাউন্ডে কাজ করার জন্য ওয়ার্কার তৈরি
    const worker = new Worker('./workers/worker.js');

    // ওয়ার্কার থেকে মেসেজ আসলে রেসপন্স পাঠানো
    worker.on('message', data => {
        res.json({
            message: `Heavy computation done! Result: ${data}`,
        });
    });

    worker.on('error', err => {
        res.status(500).json({ error: 'Worker crashed!' });
    });
});
```

### 2. Worker File (`worker.js`)

এখানে আসল ভারী ক্যালকুলেশনটি করা হচ্ছে যা মেইন থ্রেডকে ব্লক করবে না।

```javascript
import { parentPort } from 'worker_threads';

function doCalculation() {
    let sum = 0;
    // ১০ বিলিয়ন পর্যন্ত লুপ (CPU Intensive)
    for (let i = 0; i < 10000000000; i++) {
        sum += i;
    }
    return sum;
}

// মেইন থ্রেডে রেজাল্ট পাঠানো
parentPort.postMessage(doCalculation());
```

---

## 🌟 Cluster vs Worker Threads (তফাত কি?)

- **Cluster:** এটি মূলত মাল্টিপল প্রসেস তৈরি করে। প্রতিটি প্রসেসের আলাদা মেমোরি থাকে। এটি স্কেলিং এবং ইনকামিং রিকোয়েস্ট ডিস্ট্রিবিউট করার জন্য পারফেক্ট।
- **Worker Threads:** এটি একটি প্রসেসের ভেতরেই মাল্টিপল থ্রেড তৈরি করে এবং এরা মেমোরি শেয়ার করতে পারে। এটি কেবল CPU-heavy টাস্কের জন্য ব্যবহৃত হয়।

---

## ✅ Benefits of Worker Threads

- **Non-blocking Request:** ভারী কাজের সময়ও সার্ভার রেসপন্সিভ থাকে।
- **Shared Memory:** মেমোরি শেয়ার করার সুবিধা পাওয়া যায় (SharedArrayBuffer)।
- **Multi-core Performance:** প্রসেসরের সব কোরকে দক্ষভাবে ব্যবহার করা যায়।

---

## 📊 Summary

Node.js Worker Threads মূলত ব্যাকএন্ডের কর্মক্ষমতা বাড়ানোর একটি শক্তিশালী টুল। এটি ব্যবহার করলে এপিআই কখনও স্লো বা আন-রেসপন্সিভ হবে না, যত বড় ক্যালকুলেশনই করা হোক না কেন।

### Short Note:

- Cluster : Multiple Process বা Multiple CPU Core ব্যাবহার করে Multiple সার্ভার ইন্সটেন্স তৈরি করে। যা মূলত লোড ব্যালেন্সিং এর জন্য ব্যবহৃত হয়। এবং এতে একটা প্রসেস ক্র্যাশ করলে বা ব্যাস্ত থাকলে বাকি প্রসেসগুলো কাজ করতে থাকে।

- Worker Threads : Worker Threads মূলত একটা প্রসেসের মধ্যে কোন হেভি কম্পিউটেশনাল টাস্ক বা CPU-Intensive টাস্ক হলে মেইন থ্রেডকে ব্লক না করে অন্য একটি Thread ওপেন করে সেখানে কাজ করা এবং মেইন থ্রেড কে ফ্রি রাখা।

- libuv : libuv হলো একটি C লাইব্রেরি যা Node.js কে অ্যাসিঙ্ক্রোনাস I/O অপারেশন করতে সাহায্য করে। এটি Node.js এর ইভেন্ট লুপ এবং ফাইল সিস্টেম, নেটওয়ার্কিং, এবং অন্যান্য I/O অপারেশন পরিচালনা করে।

- libuv Thread Pool : libuv Thread Pool হলো একটি পুল যেখানে কিছু থ্রেড (ডিফল্ট ৪টি) থাকে যারা I/O অপারেশনগুলো পরিচালনা করে।

